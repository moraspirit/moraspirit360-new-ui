services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: moraspirit360-ui:local
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      RUN_PRISMA_MIGRATIONS: "true"
